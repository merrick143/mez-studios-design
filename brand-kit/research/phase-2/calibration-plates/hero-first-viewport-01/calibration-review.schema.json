{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mez.systems/schemas/hero-first-viewport-01-review.schema.json",
  "title": "Mez hero first viewport calibration review",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "studyId", "exportedAt", "complete", "productionAuthority", "sourceExpressionApproved", "decisions"],
  "properties": {
    "schemaVersion": {"const": "1.0.0"},
    "studyId": {"const": "MEZ-HERO-FIRST-VIEWPORT-01"},
    "exportedAt": {"type": "string", "format": "date-time"},
    "complete": {"type": "boolean"},
    "productionAuthority": {"const": false},
    "sourceExpressionApproved": {"const": false},
    "decisions": {
      "type": "object",
      "additionalProperties": false,
      "required": ["overallDirection", "heroComposition", "typography", "controls", "mobileOpening"],
      "properties": {
        "overallDirection": {"$ref": "#/$defs/decision"},
        "heroComposition": {"$ref": "#/$defs/decision"},
        "typography": {"$ref": "#/$defs/decision"},
        "controls": {"$ref": "#/$defs/decision"},
        "mobileOpening": {"$ref": "#/$defs/decision"}
      }
    },
    "overallNote": {"type": "string"}
  },
  "$defs": {
    "decision": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision", "note"],
      "properties": {
        "decision": {"enum": ["advance", "edit", "reject"]},
        "note": {"type": "string"}
      }
    }
  }
}
