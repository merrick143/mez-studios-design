# AI OS screenshot curation

Status: `candidate evidence pool`

Production authority: `false`

The detailed source, privacy and crop data lives in `manifest.json`. This file is the fast human and agent routing layer.

## Primary native proof

Use these before device mockups when the job is to prove the real product:

1. `aios-home-dashboard` is the strongest broad AI OS overview. It connects quick links, CEO tasks and people in one clean full-width view.
2. `aios-hubs-directory` is the strongest proof of system breadth. It shows operating hubs, people and clients on one surface.
3. `aios-documentation-hub` is the strongest context-engine proof. Preserve its centred Notion width and redact the user avatar before public use.
4. `aios-stride-system` is the strongest method proof. It makes the 12-week operating loop tangible and also needs avatar redaction.

The older `aios-stride-okrs-kpis`, `aios-stride-projects-archive` and `aios-weekly-sprint-notion-ai` captures are supporting evidence. They contain extra app chrome, site-setting banners or more sensitive working detail and should not lead the next ad batch.

## Primary supplied compositions

- `aios-mac-studio-dashboard`: strongest desktop device object.
- `aios-macbook-stride`: strongest execution-system device object.
- `aios-iphone-dual`: strongest wide mobile device composition.
- `aios-iphone-dual-black-titanium`: strongest portrait mobile device composition.

Use supplied compositions when the device context materially improves the ad. Native proof should remain the default because the interface stays larger and more credible.

## Hold

- `aios-macbook-square`: the product screen is too small to carry proof.
- `aios-mac-studio-monitor`: the product screen is too dark for reliable use.

## Public-use gate

No asset is approved-public. Before the next static batch:

1. choose the capture and channel crop from `manifest.json`;
2. review every visible name, avatar, task, client and commercial value at full resolution;
3. create a separate redacted derivative where required;
4. record the derivative and approval state before upload to Figma.

The `public-safe-candidate` label means visually promising after an initial review, not approved for publishing.
