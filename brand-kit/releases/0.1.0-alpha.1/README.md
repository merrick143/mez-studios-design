# Mez Systems 0.1.0-alpha.1

This is the canonical-migration identity snapshot, not the foundation-complete production release. It contains the literal five-product registry, complete source-backed gradient library, exact static twins, canonical Wings, shared Living Core renderer, approved decision ledger and schemas.

Run `python3 verify.py` from this directory. The verifier uses only the Python standard library.
