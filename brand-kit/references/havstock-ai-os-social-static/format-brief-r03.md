# AI OS 4:5 social-static format brief · R03

Status: candidate

Production authority: false

Task: `FIGMA-PLAYGROUND-AI-OS-STATIC-R03`

## Human correction

R02 used a real approved Backend proof asset, but it did not use the stronger product screenshots
already embedded on the Playground page. R03 reuses those Figma image assets directly. It removes the
guarantee-condition footnote, fixes the qualifier to hug its content, adds a sentence-level Notion
identity label, and introduces the requested faint grid and restrained downward top wash.

## Locked construction

- Canvas: 1080 by 1350, canonical charcoal, clipped.
- Background: one 54px faint grid plus one soft light wash descending from the top edge.
- Safe area: 64px left and right.
- Qualifier: content-hugging with equal 16px horizontal padding; no fixed empty space on the right.
- Headline: the same two-line burden and replacement hooks as R02, beginning beside MZ-G13 and exact
  Wings.
- Mechanism: `Built inside` followed by a content-hugging Notion mark and `Notion OS` label, then
  `so AI starts with your business in context.` A second line names connected tasks, projects,
  clients and decisions.
- Risk reversal: `Instant access` and `14-day setup guarantee`. No asterisk and no policy footnote.
- CTA: `Get the AI OS · mez.systems`.
- Proof: five distinct real product screenshots already embedded on Playground, kept near native
  scale and cropped for readable operating evidence.
- Outcome layer: three screenshot-specific, truthful annotations. No payment, booking, member or
  revenue notifications.

## Five executions

1. `Every new chat starts blank.` / `Give AI a place to start.`
   - Screenshot: `notion-dashboard`, hash `93c5c07aa675fd58a8786e6a522d7c105b8676ab`
   - Outcomes: `Work connected`; `Active tasks`; `People + priorities`
2. `Another isolated AI window.` / `Use the layer you control.`
   - Screenshot: `notion-sops`, hash `b84ea1c1421f7e8639e62c7f85d5851e67ff2bcc`
   - Outcomes: `Processes indexed`; `24 SOPs`; `One source of truth`
3. `The tools keep changing.` / `Stop rebuilding for them.`
   - Screenshot: `notion-content`, hash `d89171579dfb15110398f3c359d2dddf19771d5e`
   - Outcomes: `Ideas captured`; `Status visible`; `Ready to produce`
4. `Generic AI advice.` / `Ground it in active work.`
   - Screenshot: `notion-hubs`, hash `015d4e4ed4129aa8ecb74511c07f2e8824608267`
   - Outcomes: `Work organised`; `Connected hubs`; `Docs + templates`
5. `Another Notion template?` / `Use the system we run.`
   - Screenshot: `notion-docs`, hash `2bfb39adb3cdcd6b060a2b6abdd2d7ffde1d26ce`
   - Outcomes: `Knowledge reusable`; `Docs + SOPs`; `Decisions remembered`

## Authority boundary

The image hashes are existing product screenshots in the user-provided Figma Playground file. Reuse
stays inside that file; no local duplicate is required. The Notion mark resolves from the sanctioned
third-party-mark asset already used in R02. MZ-G13 and Wings retain their canonical assignments.

The top wash is a bounded, human-directed candidate exception, not a new Mez foundation rule. Paid
advertising remains deferred and uncertified. These frames are Playground candidates for Olli's
review, not approved campaign assets.
